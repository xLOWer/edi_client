<Window x:Class="EdiClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EdiClient"
        xmlns:services="clr-namespace:EdiClient.Services"
        xmlns:settings="clr-namespace:EdiClient.AppSettings"
        mc:Ignorable="d" Name="mainWindow"
        Height="Auto" Width="Auto" WindowState="Maximized">
    <Window.Resources>
        <Style x:Key="lname">
            <Setter Property="Control.Margin" Value="0" />
            <Setter Property="Control.Padding" Value="0" />
            <Setter Property="Control.FontSize" Value="12" />
        </Style>
        <Style x:Key="lvalue">
            <Setter Property="Control.Margin" Value="0" />
            <Setter Property="Control.Padding" Value="0" />
            <Setter Property="Control.FontWeight" Value="Bold" />
            <Setter Property="Control.FontSize" Value="12" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="26"/>
            <RowDefinition Height="26"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="22"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>



        <Menu Name="TopMenu1" Grid.ColumnSpan="2" Grid.Column="0" Grid.Row="0"  Grid.IsSharedSizeScope="True">
            <MenuItem Header="Настройки"  x:Name="CommonSettings" Command="{Binding OpenCommonSettingsCommand}" />
            <MenuItem Header="О программе">
                <MenuItem x:Name="License" Header="Лицензия" Click="License_Click" />
                <MenuItem x:Name="About" Header="О программе" Click="About_Click"/>
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="26"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="390"/>
                <ColumnDefinition Width="28"/>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ComboBox ItemsSource="{Binding Path=(services:EdiService.Relationships), Mode=TwoWay}" 
                        SelectedIndex="0" BorderThickness="0" Background="Transparent" Grid.Column="0"
                        SelectedItem="{Binding Path=(services:EdiService.SelectedRelationship), Mode=TwoWay}"
                        DisplayMemberPath="Name" x:Name="RelationshipsComboBox"
                        SelectedValuePath="Name"/>

            <Button ToolTip="Обновить список контрагентов" Grid.Column="1" Margin="2,2" BorderBrush="Transparent" Background="Transparent" BorderThickness="0"
                    Command="{Binding RefreshRelationshipsCommand}" CommandParameter="{Binding ElementName=RelationshipsComboBox}">
                <Image Source="~\..\Images\refresh-50.png" Margin="-1"/>                
            </Button>
            <!--<Label Content="- нажми если нет нужного клиента" Grid.Column="2"/>-->
        </Grid>



        <TabControl Name="MainTabControl" Grid.Row="2" BorderBrush="Transparent" BorderThickness="0"
                    ItemsSource="{Binding Source={x:Static services:TabService.Tabs}}"
                        SelectedIndex="0">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Height="21" Width="Auto">
                        <TextBlock Width="Auto" Text="{Binding Title}"/>
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <Frame Content="{Binding Content}" Width="Auto" Height="Auto" NavigationUIVisibility="Hidden"/>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>

        <StatusBar Grid.Row="3" >
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Label Content="пользователь: " Style="{StaticResource lname}"/>
                    <Label Content="{Binding Path=(settings:AppConfig.DbUserName)}" Style="{StaticResource lvalue}"/>
                </StackPanel>
            </StatusBarItem>

            <Separator/>

            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Label Content="хост: " Style="{StaticResource lname}"/>
                    <Label Content="{Binding Path=(settings:AppConfig.DbHost)}" Style="{StaticResource lvalue}"/>
                </StackPanel>
            </StatusBarItem>

            <Separator/>

            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Label Content="EDI GLN: " Style="{StaticResource lname}"/>
                    <Label Content="{Binding Path=(settings:AppConfig.EdiGLN)}" Style="{StaticResource lvalue}"/>
                </StackPanel>
            </StatusBarItem>

            <Separator/>

            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Label Content="Польз-ль трейдера: " Style="{StaticResource lname}"/>
                    <Label Content="{Binding Path=(settings:AppConfig.TraderUserName)}" Style="{StaticResource lvalue}"/>
                </StackPanel>
            </StatusBarItem>

            <Separator/>

            <!--<StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Label Content="Логирование: " Style="{StaticResource lname}"/>
                    <Label Content="{Binding Path=(settings:AppConfig.EnableLogging)}" Style="{StaticResource lvalue}"/>
                </StackPanel>
            </StatusBarItem>

            <Separator/>
            
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Label Content="Автообработка: " Style="{StaticResource lname}"/>
                    <Label Content="{Binding Path=(settings:AppConfig.EnableAutoHandler)}" Style="{StaticResource lvalue}"/>
                </StackPanel>
            </StatusBarItem>
            
            <Separator/>-->
            
            <StatusBarItem>
                <Label Content=""/>
            </StatusBarItem>

        </StatusBar>

    </Grid>
</Window>
