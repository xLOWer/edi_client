<Page x:Class="EdiClient.View.MatchMakerView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EdiClient.View"
      mc:Ignorable="d" 
d:DesignHeight="1080" d:DesignWidth="1920"
      Title="MatchMaker">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="0.02*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="0.02*"/>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="0.02*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        
        
        
        <DataGrid Name="GoodsDataGrid" 
                  ItemsSource="{Binding Path=Documents,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                  SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  
                  SelectionMode="Single"
                  Grid.Column="0"    
                  Grid.IsSharedSizeScope="True" 
                  AutoGenerateColumns="False" 
                  SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding DespatchAdviceParties.DeliveryPoint.ILN}" Width="*" Header="ID"/>
                <DataGridTextColumn Binding="{Binding DespatchAdviceParties.Buyer.ILN}" Width="3*" Header="Наименование"/>
                <DataGridTextColumn Binding="{Binding DespatchAdviceParties.UltimateCustomer.ILN}" Width="*" Header="Код"/>
                <DataGridTextColumn Binding="{Binding DespatchAdviceParties.Seller.ILN}" Width="*" Header="Штрих-код"/>
            </DataGrid.Columns>
        </DataGrid>
        
        
        
        
        
        <DataGrid Name="FailerDataGrid" 
                  ItemsSource="{Binding Path=Documents,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                  SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  
                  SelectionMode="Single"
                  Grid.Column="2"    
                  Grid.IsSharedSizeScope="True" 
                  AutoGenerateColumns="False" 
                  SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding DespatchAdviceParties.DeliveryPoint.ILN}" Width="*" Header="ID"/>
                <DataGridTextColumn Binding="{Binding DespatchAdviceParties.Buyer.ILN}" Width="3*" Header="Наименование"/>
                <DataGridTextColumn Binding="{Binding DespatchAdviceParties.UltimateCustomer.ILN}" Width="*" Header="Код"/>
                <DataGridTextColumn Binding="{Binding DespatchAdviceParties.Seller.ILN}" Width="*" Header="Штрих-код"/>
            </DataGrid.Columns>
        </DataGrid>
        
        
        
        
        
        <Grid Grid.Column="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="0.2*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="0.2*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="0.2*"/>
                <RowDefinition Height="10*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            
            <Button Content="Связать" Grid.Row="2" Grid.Column="1"
                    Command="{Binding MakeMatchingCommand}"/>
            
            <Button Content="Отвязать" Grid.Row="4" Grid.Column="1"
                     Command="{Binding DisposeMatchingCommand}"/>
            
        </Grid>


        
        


        <DataGrid Name="MatchedDataGrid" 
                  ItemsSource="{Binding Path=Documents,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                  SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  
                  SelectionMode="Single"
                  Grid.Column="6"    
                  Grid.IsSharedSizeScope="True" 
                  AutoGenerateColumns="False" 
                  SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding DespatchAdviceParties.DeliveryPoint.ILN}" Width="*" Header="ID"/>
                <DataGridTextColumn Binding="{Binding DespatchAdviceParties.Buyer.ILN}" Width="3*" Header="Наименование"/>
                <DataGridTextColumn Binding="{Binding DespatchAdviceParties.UltimateCustomer.ILN}" Width="*" Header="Код"/>
                <DataGridTextColumn Binding="{Binding DespatchAdviceParties.Seller.ILN}" Width="*" Header="Штрих-код"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Page>
